<root>
    <struct name="CameraMatrix" cpp="hgl/graph/CameraMatrix.h">
        mat4 ortho;

        mat4 projection;
        mat4 inverse_projection;

        mat4 view;
        mat4 inverse_view;
        mat4 normal;

        mat4 vp;
        mat4 inverse_vp;

        vec3 pos;
        vec3 target;
        vec3 world_up;

        vec3 view_line;
        vec3 camera_direction;
        vec3 camera_right;
        vec3 camera_up;
        vec4 view_distance;

        float znear,zfar;

        vec2 canvas_resolution;
        vec2 viewport_resolution;
        vec2 inv_viewport_resolution;
    </struct>

    <struct name="MVPMatrix" cpp="hgl/graph/CameraMatrix.h">
        mat4 l2w;
        mat4 inverse_l2w;

        mat4 mvp;
        mat4 inverse_mvp;
    </struct>
</root>
