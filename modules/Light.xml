<root>

    <struct name="DirectionLight">
        vec4 direction;
    </struct>

    <struct name="PointLight">
        vec4 position;
        float constant;
        float linear;
        float quadratic;
    </struct>

    <struct name="SpotLight">
        vec3 position;
        vec3 direction;
        float cut_off;
        float outer_cut_off;
    </struct>

</root>
