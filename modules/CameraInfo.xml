<root>
    <struct name="CameraInfo" cpp="hgl/graph/CameraInfo.h">
        mat4 ortho;

        mat4 projection;
        mat4 inverse_projection;

        mat4 view;
        mat4 inverse_view;
        mat4 normal;

        mat4 vp;
        mat4 inverse_vp;

        vec3 pos;
        vec3 target;
        vec3 world_up;

        vec4 view_line;             //pos-target, .w is length(.xyz)
        vec4 camera_direction;      //.w is length(.xyz)
        vec4 camera_right;          //.w is length(.xyz)
        vec4 camera_up;             //.w is length(.xyz)

        float znear,zfar;

        vec2 canvas_resolution;
        vec2 viewport_resolution;
        vec2 inv_viewport_resolution;
    </struct>
</root>
