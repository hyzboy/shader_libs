<root>
    <struct name="PhongMaterial">
        vec3 ambient;
        vec3 diffuse;
        vec3 specular;
        float shininess;
    </struct>

    <struct name="PhongLightColor">
        vec3 ambient;
        vec3 diffuse;
        vec3 specular;
    </struct>

    <struct name="PhongDirectionLight">
        vec3 direction;

        PhongLightColor color;
    </struct>

    <struct name="PhongPointLight">
        vec3 position;

        PhongLightColor color;

        float constant;
        float linear;
        float quadratic;
    </struct>

    <struct name="PhongSpotLight">
        vec3 position;
        vec3 direction;

        PhongLightColor color;

        float cut_off;
        float outer_cut_off;
    </struct>
</root>
